package com.cyberway.core.objects;

import com.cyberway.core.utils.PropsUtil;
import com.cyberway.core.utils.StringUtil;

public class FlowConstants {
    
    //  是否使用流程，从system.xml中读取
    public static final boolean USE_FLOW=new Boolean(PropsUtil.getInstance().getProperty("workflow.useFlow"));
    //流程包ids key
    public static final String FLOW_PACKAGEIDS_KEY = "FLOW_PACKAGEIDS_KEY";//流程包ID KEY
    public static final String FLOW_PROCESSMGRS_KEY = "FLOW_PROCESSMGR_KEY";//流程管理名(所有能启动的流程) KEY
    //流程配置实际路径
    public static final String FLOW_CONFIG_REAL_PATH = PropsUtil.getInstance().getProperty("workflow.defpath");
    //默认的流程id
    public static final String DEFUALT_FLOW_USERID = PropsUtil.getInstance().getProperty("workflow.defaultUserId");
}
