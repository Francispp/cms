package com.cyberway.crawl.extractor.service;

import com.cyberway.core.dao.HibernateEntityDao;
import com.cyberway.crawl.extractor.domain.ExtractLog;

public class ExtractLogManagerService extends HibernateEntityDao<ExtractLog>{
	
	
	/*public ExtractLog saveOrUpdate(ExtractLog extractLog){
		Boolean unique = this.isNotUnique(extractLog, "keyField,channelId");
		if(unique)
			return null;//throw new BizException("数据已经存在！");
		super.saveOrUpdate(extractLog);
		return extractLog;
	}*/
	
	

}
